# 📁 assets — راهنمای نگاره‌ها و صداهای نورستان

همهٔ تصویرها و صداهای بیرونی این‌جا می‌نشینند. **هیچ‌کدام شرط اجرای برنامه نیستند**:
اگر فایلی نبود، همان نگارهٔ SVG درون‌خطی (`Art.*`) جایش را می‌گیرد و برنامه
«تصویر شکسته» نشان نمی‌دهد. پس این پوشه را می‌توانی کم‌کم و به‌تدریج پر کنی.

> **استثنا: آیکن‌های برنامه.** پوشهٔ `images/icons/` دیگر خالی نیست و نباید
> خالی باشد. مرورگر برای پیشنهاد نصب به یک آیکن ≥۱۹۲ پیکسلِ **قابل دریافت**
> نیاز دارد و Safari برای `apple-touch-icon` آیکن برداری را نمی‌پذیرد. این
> فایل‌ها با `node _icons.js` از نو ساخته می‌شوند:
>
> | فایل | اندازه | کاربرد |
> |---|---|---|
> | `icon-192.png` | ۱۹۲×۱۹۲ | شرط نصب کروم |
> | `icon-512.png` | ۵۱۲×۵۱۲ | صفحهٔ نصب و اسپلش |
> | `maskable-512.png` | ۵۱۲×۵۱۲ | برش دایره‌ای اندروید (حاشیهٔ امن ۱۴٪) |
> | `apple-touch-icon.png` | ۱۸۰×۱۸۰ | iOS؛ SVG را نمی‌پذیرد |
> | `favicon-32.png` | ۳۲×۳۲ | زبانهٔ مرورگر |
> | `icon.svg` | برداری | دست‌نویس؛ اعدادش با `icon-192.png` یکی است |
>
> اگر شکل «ن» را عوض کردی، هم `_icons.js` را بساز و هم `icon.svg` را
> دستی به‌روز کن — سنجش‌ها واگرایی این دو را می‌گیرند.

---

## ۱. درخت پوشه‌ها

```
assets/
├── fonts/               قلمِ پایهٔ برنامه (Vazirmatn) — ✅ موجود
├── images/
│   ├── icons/           شمایل برنامه و PWA — ✅ موجود
│   ├── splash/          پردهٔ آغازین — ⬜ خالی، Art جایش را گرفته
│   ├── patterns/        کاشی و بافت بی‌درز — ⬜ خالی
│   ├── illustrations/   نگاره‌های بزرگ (سربرگ‌ها) — ⬜ خالی
│   ├── surahs/
│   │   └── thumbs/      تصویر بندانگشتی هر سوره — ⬜ خالی، مدال گرادیانی
│   └── avatars/         چهرهٔ قاریان — ⬜ خالی، چهرهٔ گرادیانی
└── audio/
    ├── ui/              صداهای ریز رابط — ⬜ خالی، Web Audio
    └── ambient/         صدای پس‌زمینه — ⬜ خالی، سکوت
```

> پوشه‌های ⬜ عمداً خالی‌اند و git نگهشان نمی‌دارد (git پوشهٔ خالی را
> track نمی‌کند). نبودنشان هیچ چیزی را نمی‌شکند: نگارهٔ SVGِ درون‌خطی و
> موتور صوتی Web Audio کار را راه می‌برند. فقط وقتی فایل واقعی گذاشتی
> پوشه دوباره سرِ جایش می‌آید. دلیلِ آنکه فقط `icons/` و `fonts/` پرشده‌اند
> در بالای همین فایل آمده: آن دو شرطِ نصب PWA و خوانایی متن‌اند، نه تزیین.

---

## ۲. جدول فایل‌ها

| مسیر | اندازهٔ پیشنهادی | کاربرد | اگر نبود |
|---|---|---|---|
| **موجودها** | | | |
| `fonts/vazirmatn-regular.woff2` | ۵۱KB | وزن ۴۰۰ — متنِ همه‌جای برنامه | قلمِ پیش‌فرضِ سیستم |
| `fonts/vazirmatn-bold.woff2` | ۵۱KB | وزن ۷۰۰ — دکمه‌ها و `b` | ضخیم‌سازیِ مصنوعیِ مرورگر |
| `fonts/vazirmatn-extrabold.woff2` | ۵۱KB | وزن ۸۰۰ — تیترها (`--f-disp-w`) | ضخیم‌سازیِ مصنوعیِ مرورگر |
| `images/icons/icon-192.png` | ۱۹۲×۱۹۲ | شرط نصب کروم و میان‌برهای مانیفست | نصب پیشنهاد نمی‌شود |
| `images/icons/icon-512.png` | ۵۱۲×۵۱۲ | صفحهٔ نصب و اسپلش | نصب پیشنهاد نمی‌شود |
| `images/icons/maskable-512.png` | ۵۱۲×۵۱۲ | برش دایره‌ای اندروید | آیکن با گوشهٔ بریده |
| `images/icons/apple-touch-icon.png` | ۱۸۰×۱۸۰ | iOS؛ SVG را نمی‌پذیرد | آیکن پیش‌فرض iOS |
| `images/icons/favicon-32.png` | ۳۲×۳۲ | زبانهٔ مرورگر | آیکن پیش‌فرض مرورگر |
| `images/icons/icon.svg` | برداری | مانیفست (purpose: any) | — |
| **اختیاری‌ها — هر وقت خواستی بگذار** | | | |
| `images/splash/splash.webp` | 1080×1920 | پس‌زمینهٔ پردهٔ آغازین | `Art.mushaf()` |
| `images/patterns/tile.webp` | 380×380 | کاشی بی‌درز پشت برنامه | `Art.patternTile()` |
| `images/illustrations/mushaf.webp` | 900×1200 | قرآن گشوده | `Art.mushaf()` |
| `images/illustrations/panorama.webp` | 1600×900 | نمای مسجد شبانه | `Art.panorama()` |
| `images/surahs/thumbs/001.webp` … `114.webp` | ۳۰۰×۳۰۰ | بندانگشتی سوره (شمارهٔ سه‌رقمی) | مدال گرادیانی |
| `images/avatars/reciter-1.webp` … `reciter-10.webp` | ۱۲۸×۱۲۸ | چهرهٔ قاری | چهرهٔ گرادیانی |
| `audio/ui/click.mp3` | < ۲۰KB | ضربهٔ دکمه | موتور صوتی Web Audio |
| `audio/ambient/night.mp3` | ۱–۳MB | صدای آرام شب | سکوت |

> **شمارهٔ سه‌رقمی سوره:** `001.webp` تا `114.webp` — با صفر ابتدایی، تا ترتیب
> الفبایی فایل‌ها با ترتیب سوره‌ها یکی باشد.
>
> **هشدار نسخهٔ ۱۷:** در ستون «مسیر» تا پیش از این نسخه نام‌هایی بود که
> وجود نداشتند و کد هم به‌زور سراغشان می‌رفت — مثلاً برای هر ده قاری یک
> `avatars/reciter-N.webp` درخواست می‌شد و هر ده تا ۴۰۴ می‌گرفتند.
> حالا جدول بالا **همان چیزی است که هست**، و کد هم فقط چیزی را می‌خواهد
> که در جدول «اختیاری» با همان نام آمده باشد. برای قاری، نشانی را در خودِ
> دادهٔ `RECITERS` بگذار (`img:'avatars/reciter-1.webp'`)، نه در کد.

---

## ۳. قالب فایل‌ها

- **تصویر:** `WebP` در اولویت، بعد `JPG`. توجه: واپس‌رویِ خودکار بین دو
  پسوند **در `srcset` نیست** — هر دو نامزد با توصیفگر ۱x می‌آمدند و مرورگر
  فقط اولی را برمی‌داشت. واپس‌رویِ واقعی این است: اگر تصویر بار نشد،
  نگارهٔ SVGِ درون‌خطی جایش می‌نشیند.
- **صدا:** `MP3` (۱۲۸kbps کافی است) یا `OGG`.
- **نکتهٔ اندروید:** نام فایل‌ها را **انگلیسی و بدون فاصله** بگذار
  (`reciter-1.webp` نه `قاری ۱.webp`).
- **شفافیت:** آیکن‌ها PNG/WebP با آلفا یا SVG؛ پس‌زمینه‌ها JPG (سبک‌تر).

---

## ۴. قرارداد کد — `Assets.imageOrSvg`

تنها راه خواندن تصویر در برنامه همین است:

```js
Assets.imageOrSvg('illustrations/mushaf.webp', Art.mushaf(), { alt: 'قرآن گشوده' })
```

- `مسیر` — نسبی، از پوشهٔ `assets/images/` حساب می‌شود.
- `نگارهٔ SVG` — همان رشته‌ای که `Art.*` می‌سازد؛ **جانشین** تصویر است.
- اگر تصویر نبود یا شبکه قطع بود، یک شنوندهٔ سراسری `error` در `Assets.watch()`
  خودکار جای `<img>` را با SVG پر می‌کند. هیچ‌جا `onerror` دستی لازم نیست.

توابع کمکی دیگر:

| تابع | کار |
|---|---|
| `Assets.img(path, {alt, w, h, eager})` | `<img loading="lazy" decoding="async">`؛ بی `srcset` |
| `Assets.path(p, 'images')` | نرمال‌سازی مسیر و افزودن پیشوند `assets/` |
| `Assets.variants(p)` | نام‌های واریانت `webp`/`jpg` — برای وقتی خودت `<picture>` می‌سازی |
| `Assets.avatar(name)` | چهرهٔ گرادیانی قطعی از نام (بدون فایل) |
| `Assets.svg(markup)` | پوشاندن SVG درون‌خطی در یک `span` |

`image-set()` در CSS هم پشتیبانی می‌شود؛ برای پس‌زمینه‌ها از همان
`Assets.path` استفاده کن تا مسیر یک‌جا بماند.

---

## ۵. کش و آفلاین

`sw.js` (نسخهٔ کش: `noorestan-20`) این پوشه را این‌گونه مدیریت می‌کند:

| گروه | راهبرد |
|---|---|
| پوستهٔ برنامه (`index.html`, `manifest.json`) | پیش‌ذخیره، اول شبکه |
| `assets/**` | اول کش + تازه‌سازی پس‌زمینه |
| فونت‌های jsDelivr | اول کش + تازه‌سازی پس‌زمینه |
| متن قرآن (`api.alquran.cloud`) | اول شبکه، بی‌شبکه از کش |
| صوت قاریان (`everyayah`, `islamic.network`) | **بی رهگیری** — کامل به مرورگر واگذار |

> شاخهٔ صوت عمداً هیچ `respondWith` ندارد؛ شرحش بالای `isMedia()` در `sw.js`
> هست. خلاصه: پاسخِ `<audio>` مبهم (opaque) است و هر لایه‌ای که آن را بسنجد
> پخش را می‌شکند.

سقف کش: ۱۲۰ فایل رسانه‌ای و ۲۶۰ پاسخ متنی؛ قدیمی‌ترین‌ها خودکار می‌روند.
برای پاک کردن دستی، در کنسول مرورگر بنویس:

```js
navigator.serviceWorker.controller.postMessage({ t: 'clear' })
```

---

## ۶. ساخت تصویرها با چه چیزی؟

- **نگاره‌های قرآنی:** چون برنامه هیچ فایل بیرونی **لازم** ندارد، همهٔ نگاره‌های
  فعلی SVG خالص‌اند (`Art.mushaf`, `Art.panorama`, `Art.patternTile`, `Art.band`,
  `Art.rays`, `Art.ornament`). عکس واقعی را با `Assets.imageOrSvg(مسیر, Art.توابع)`
  جای نگاره بگذار تا اگر بار نشد، همان SVG بماند.
- **قاریان:** بهتر است عکس واقعی **نباشد**؛ چهرهٔ گرادیانی از `hue` خودِ قاری
  ساخته می‌شود و مشکل حق تصویر ندارد. اگر عکس داری، فایل را در
  `images/avatars/` بگذار و نشانی‌اش را در دادهٔ `RECITERS` اضافه کن
  (`img:'avatars/reciter-1.webp'`). بی آن فیلد، هیچ درخواستِ تصویری برای
  قاری فرستاده نمی‌شود — پیش‌تر می‌شد و همه‌اش ۴۰۴ بود.
- **فشرده‌سازی:** هر تصویر را زیر ۲۰۰KB نگه دار. برای فشرده‌سازی در اندروید:
  `cwebp -q 78 in.png -o out.webp`


## نگاره‌ها و قلمِ تیتر (به‌روزرسانی ۲۰)

- `fonts/lalezar-arabic-400.woff2`: لاله‌زار، بستهٔ @fontsource/lalezar نسخهٔ 5.3.0؛ مجوز SIL OFL در `fonts/Lalezar-OFL.txt`.
- `images/promo/courtyard.svg`، `recitation.svg` و `night.svg`: نگاره‌های هندسی/معماریِ دست‌ساز این پروژه؛ بدون عکس، فونت یا لینک خارجی.
- هر چهار فایل در service worker پیش‌ذخیره می‌شوند؛ اسلایدر به Swiper یا CDN نیاز ندارد.
