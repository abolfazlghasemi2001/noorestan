# راهنمای اسکرین‌شات نورستان

این فایل جای `screenshots/README.md` را به‌عنوان مرجع اصلی می‌گیرد: فهرست
نشانی‌ها، چک‌لیست عکس‌ها، و راه اجرا روی رایانه.

> **روی همین گوشی، عکس‌گرفتن خودکار ممکن نیست.** دلیلش کوتاه: Puppeteer
> برای اندروید/arm64 هیچ نسخه‌ای از Chrome ندارد و باینری‌های موجود هم
> glibc‌محورند و روی Bionic اجرا نمی‌شوند. پس روی گوشی **دستی** عکس بگیر
> (بخش ۳)، و برای عکس‌برداری خودکار به رایانه برو (بخش ۴).

---

## ۱. نشانی‌های نورستان

سرور را بالا بیاور و برنامه را از راه سرور باز کن:

```bash
bash start.sh          # یا: node server.js
# پیش‌فرض: http://localhost:8787
```

> **هرگز با `file://` باز نکن.** در بافت غیرامن، `crypto.subtle` تعریف
> نشده است و ورود مدیر و ذخیرهٔ متن سوره‌ها کار نمی‌کند. آدرس را از
> `http://localhost:8787/index.html` بگیر.

### الف) صفحه‌هایی که نشانی مستقیم دارند

Router هنگام بارگذاری هش را می‌خواند، پس این‌ها را می‌شود مستقیم زد:

| نشانی | صفحه |
|---|---|
| `#home` | خانه |
| `#online` | محفل |
| `#notif` | اعلان |
| `#admin` | پنل مدیر (رمز پیش‌فرض `noor2024`) |
| `#room` | روم — **بی‌روم خالی می‌ماند**، بخش ۳ را ببین |
| `#quran` | تلاوت قرآن |
| `#read` | مصحف‌نما |
| `#play` | همهٔ بازی‌ها |
| `#me` | «من» — تب پیش‌فرض «آمار» است |
| `#join=CODE` | پیوند دعوت به روم |

### ب) بازی‌ها و مودال‌ها نشانی ندارند

بازی‌ها با `Games[id]()` اجرا می‌شوند، نه با مسیر. پس باید کلیک کنی:

**نوار پایین → 🎮 بازی** → کاشی بازی.
**نوار پایین → 👤 حساب** → تب‌ها: پروفایل، آمار، رکوردها، نشان‌ها، تنظیمات.

### ج) حالت اشکال‌زدایی

پارامتر پرس‌وجو است، نه هش:

```
http://localhost:8787/index.html?debug=1
```

---

## ۲. چک‌لیست عکس‌ها

بگیر، بعد کنار هر قلم تیک بزن. حجم هدف: هر عکس زیر ۲۰۰KB.

**تنظیمات دوربین/مرورگر:** ویوپورت **۳۹۰×۸۴۴** (موبایل)، چگالی ۲،
تم تیره، پیش از هر عکس **۶۰۰ms** صبر.

### صفحه‌ها

- [ ] `01-home` — خانه، کامل (سرصفحه، بازی‌ها، کارت آیهٔ روز)
- [ ] `02-online` — محفل (فهرست روم‌ها و دوستان)
- [ ] `03-notif` — اعلان
- [ ] `04-admin` — پنل مدیر (با رمز وارد شو، وگرنه دروازهٔ ورود می‌افتد)
- [ ] `05-room` — روم **با اعضا** (بخش ۳ را ببین)

### بازی‌ها (۱۰ تا)

- [ ] `06-quiz` — سوالات قرآنی
- [ ] `07-surah` — سفر سوره‌ها
- [ ] `08-scramble` — پازل حروف
- [ ] `09-memory` — حافظه
- [ ] `10-match` — تطبیق
- [ ] `11-dooz` — دوز
- [ ] `12-esmfamil` — اسم فامیل
- [ ] `13-iran` — اطلاعات ایران
- [ ] `14-meaning` — معنی کلمه
- [ ] `15-speed` — سرعت نور

> در پرامپت، ۹ نام برده شد ولی «۱۰ بازی» خواسته شد. دهمی **سرعت نور**
> گذاشته شد چون از بقیه بازی‌گونه‌تر است. اگر منظورت چیز دیگری بود،
> لانچر این‌ها را هم دارد: حدیث‌یاب، چهارده معصوم، نجوا، نهج‌البلاغه،
> آزمون معارف، چالش روزانه، گنجینه، درخت دانش، مأموریت‌ها.

### تلاوت و مودال‌ها

- [ ] `16-quran` — تلاوت قرآن (پخش‌کنندهٔ کوچک باز)
- [ ] `17-modal-settings` — ⚙️ تنظیمات (کارت‌های نصب، پیامک، فونت)
- [ ] `18-modal-stats` — 📊 آمار
- [ ] `19-modal-badges` — 🎖️ نشان‌ها
- [ ] `20-modal-leaderboard` — 🏅 رکوردها

### اشکال‌زدایی و پیامک

- [ ] `21-debug` — `?debug=1`
- [ ] `22-otp` — 📱 ورود با موبایل (گام شماره) — تنظیمات → «ورود با موبایل»
- [ ] `23-otp-code` — گام کد پنج‌رقمی (در حالت آزمایشی، کد روی صفحه نوشته می‌شود)
- [ ] `24-sms-settings` — کارت «📱 پیامک» در تنظیمات

---

## ۳. عکس‌گرفتن دستی روی همین گوشی

۱. `node server.js` را اجرا کن (یا `bash start.sh`).
۲. آدرس را در Chrome باز کن. آدرس شبکهٔ محلی را خودِ سرور چاپ می‌کند
   (مثل `http://192.168.1.5:8787`)؛ `localhost` هم روی همین گوشی کار می‌کند.
۳. با **پاور + کم‌کردن صدا** عکس بگیر.

### عکس «روم» بی روم واقعی نمی‌افتد

`renderRoom()` اگر `Net.room` نباشد بی‌درنگ برمی‌گردد و صفحه خالی می‌ماند.
دو راه:

- **واقعی:** دو دستگاه (یا دو مرورگر) را وصل کن، «ساخت روم» بزن، بعد وارد شو.
- **تک‌نفره:** در کنسول مرورگر خودت یک روم نمونه بساز:

```js
Net.room = {
  name: 'محفل دوستان', code: '482913', game: 'dooz',
  host: Net.id, spectator: false,
  members: [
    { id: Net.id, name: Store.get('playerName'), active: true },
    { id: 'd2', name: 'زهرا',  active: true },
    { id: 'd3', name: 'محمد',  active: false }
  ]
};
Router.go('room');
renderRoom();
```

### پاک‌کردن انیمیشن‌ها (تا عکس‌ها یک‌تکرار شوند)

در کنسول:

```js
document.head.appendChild(Object.assign(document.createElement('style'),
  { textContent: '*,*::before,*::after{animation:none!important;transition:none!important}' }));
```

---

## ۴. اجرا روی رایانه (خودکار)

`_shots.js` آماده است و روی رایانهٔ معمولی کار می‌کند.

```bash
# ۱. کل پوشهٔ نورستان را کپی کن (یا فقط _shots.js و _shots_env)
# ۲. نصب — روی فایل‌سیستمی که سیم‌لینک پشتیبانی می‌کند
cd _shots_env && npm install

# ۳. اجرا از ریشهٔ برنامه
cd ..
node _shots.js
```

خروجی در `screenshots/` با نام‌های شماره‌دار می‌آید. اسکریپت خودش
`server.js` را روی یک پورت آزاد بالا می‌آورد و آخرش می‌بندد — چون صفحهٔ
«محفل» و «روم» به WebSocket همان سرور وصل می‌شوند و با یک سرور ایستای
ساده در حالت قطع عکس می‌افتند.

### پرچم‌ها

| پرچم | کار |
|---|---|
| `--out=مسیر` | پوشهٔ خروجی (پیش‌فرض `screenshots`) |
| `--port=8801` | پورت سرور موقت (پیش‌فرض ۸۷۹۹) |
| `--admin-pass=رمز` | رمز پنل مدیر (پیش‌فرض `noor2024`) |
| `--all-games` | بازی‌های دیگر لانچر را هم می‌گیرد |
| `--keep-anim` | انیمیشن‌ها را نمی‌خواباند |

اسکریپت خطاهای Console را هم جمع می‌کند و در گزارش آخر چاپ می‌کند، چون
قاعدهٔ خودِ پروژه «صفر خطای Console» است. خطاهای شبکه (API قرآن، فونت
بیرونی) عمداً نادیده گرفته می‌شوند — در محیط بسته اجتناب‌ناپذیرند.

---

## ۵. چرا روی این دستگاه نشد

Puppeteer با `npm` نصب شد (۲۷ بسته، بدون خطا)، ولی خودِ مرورگر دانلود نشد:

```
Browser download failed
Error: The current platform is not supported.
```

`@puppeteer/browsers` برای اندروید/arm64 هیچ نسخه‌ای از Chrome for Testing
ندارد، پس حتی تلاش هم نمی‌کند دانلود کند. این یک مشکل مجوز یا تنظیمات نیست
که با پرچم حل شود:

| بررسی | نتیجه |
|---|---|
| معماری | `aarch64` / `arm64-v8a` |
| کتابخانهٔ C | Bionic (`/system/lib64/libc.so`) |
| glibc | نیست |
| `proot` | نصب نیست |
| سیم‌لینک در `/storage/emulated/0` | ندارد (FUSE) — `npm install` با `EACCES` می‌شکند؛ راه‌حل `--no-bin-links` |

دو مسیر اندرویدی هم آزموده و بسته شد: `screencap -p` (کد خروج ۱، صفر بایت
— مجوز فریم‌بافر ندارد) و `input tap` (تزریق ورودی ممنوع است).
