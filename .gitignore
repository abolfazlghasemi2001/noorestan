node_modules/
_shots_env/node_modules/
package-lock.json
screenshots/*.png
.DS_Store
Thumbs.db
*.swp
*~
*.log
logs/
_temp/
tmp/
.env
.env.local
.env.*
!.env.example
.vscode/
.idea/
*.bak
backup/

# ── دادهٔ سرور: هرگز نباید commit شود ──
# شامل نام کاربران، گراف دوستان و adminHash است.
# فقط با اجرای `node server.js` ساخته می‌شود و در ریشهٔ پروژه می‌نشیند.
noorestan-data.json
noorestan-data.*.json

# ── آرشیو مواد خام ──
# حدود ۵۰ مگابایت صفحهٔ اسکرپ‌شده که فقط ورودی اسکریپت‌های یک‌بارمصرف
# ساخت بانک‌اند. خروجی‌شان از قبل داخل index.html نشسته است.
# توجه: این قاعده فقط فایل‌های *تازه* را نادیده می‌گیرد؛ فایل‌هایی که
# با `git mv` به این‌جا آمده‌اند همچنان tracked می‌مانند.
archive/

# خروجی خامِ اجرای تست‌ها — نه ورودی است نه سند. سندِ واقعی همان
# _tests.js و _servertest.js است که خودشان خوانا و قابلِ اجرااند.
_o.txt
